# <skill-name> · conventions defaults

Default conventions applied when a project's `<project-root>/<skill-name>-conventions.md` doesn't specify a value. Projects can override any of these.

(For the meta-pattern, see [`meta/conventions-as-data-pattern/`](../../meta/conventions-as-data-pattern/).)

## (Convention category 1) defaults

(Default value · with rationale · with example)

## (Convention category 2) defaults

…

## When defaults apply

Per [`conventions-schema/`](../conventions-schema/) checklist, each unspecified item from the project's conventions file picks up the default from this doc.

The skill **acknowledges the source** when explaining choices:

- "Per your project's `<skill-name>-conventions.md` line N, used X"
- "Project didn't specify Y, used default from `conventions-defaults/`"
