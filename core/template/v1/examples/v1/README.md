# <skill-name> · examples

Worked walkthroughs demonstrating how to apply this skill to concrete scenarios. Each example uses **generic placeholders** (no client/project-specific identifiers) so it's reusable.

## Available examples

| Example | Scenario |
|---|---|
| (Add your first walkthrough here) | (one line) |

## How to add a new example

1. Create `<example-name>.md` in this folder
2. Follow the structure:
   - **Setup** — the concrete scenario (uses placeholders like `<role>`, `<artifact>`, `<value>`)
   - **Step-by-step walkthrough** — apply the skill's workflow
   - **Patterns demonstrated** — call out which patterns from `../patterns/` apply
   - **What went right · what to avoid**
3. Cross-reference patterns and other examples
