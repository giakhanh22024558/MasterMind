# <PROJECT NAME> — workspace onboarding

> Working folder for BA work, scaffolded by **MasterMind model_001** `/set-up`.
> Read this file when opening a new session.

## Folder layout (Core Rule)

```
<working-folder>/
├── MasterMind/        ← skill library (cloned from GitHub, read-only)
├── input/             ← raw materials (SRS docx, CR files, screenshots…)
├── context/           ← .md sidecars auto-generated by the agent (cheap to read)
├── output/            ← deliverables (.docx, .xlsx, .drawio, .json…)
├── docs/              ← artifact templates the user works on directly
└── conventions/       ← project overrides for skill defaults
```

## Standard workflow

1. **Drop raw material** into `input/` (any file format)
2. **Agent reads + creates `context/<file>.md`** sidecar (Core Rule Layer 1)
3. **User opens `docs/<artifact>.md`** and asks the agent to fill the content
4. **Agent uses the appropriate model_001 skill** — never invents the format
5. **Final deliverable** is written to `output/` (e.g. `.docx`, `.xlsx`)

## Skills available (model_001)

| Category | Skill | When to use |
|---|---|---|
| document | `requirements` | Consolidate raw input → requirements table |
| document | `features` | Backlog table — Epic / Feature / Story / AC |
| document | `analysis` | Gap + Impact Analysis for CRs |
| document | `srs` | Generate IEEE SRS docx |
| diagram | `erd` | ERD entities + relationships (Mermaid) |
| diagram | `architecture` | C4-style architecture |
| diagram | `wireframe` | Low-fi UI wireframes (annotated HTML) for new/changed screens + Design Assumptions table |
| integration | `jira` | Generate the Jira task tree from CRs/stories |
| integration | `google_sheets` | Edit Google Sheets via API (cell-level, preserves history) |
| pipeline | `business_analysis` | End-to-end orchestration of the skills above |
| utility | `setup` | (this skill) |

The agent picks the skill automatically based on the user's task description. No need to invoke by name.

## Project conventions (override defaults)

Open `conventions/<skill>-conventions.md` to override:
- ID formats (e.g. EPIC-XX vs E-XX vs Epic-XX)
- Dropdown values (Priority levels, Decision options, …)
- Color schemes
- Sheet/file naming

Leave empty = skill defaults apply.

## Artifact templates in `docs/`

| File | Backing skill | Workflow |
|---|---|---|
| `docs/project-overview.md` | `business_analysis` / `srs` | Overview master doc (tiếng Việt, format model_001) — tổng hợp & trỏ tới mọi artifact; render ra `.docx` |
| `docs/requirements.md` | `document/requirements` | REQ-XXXX table, filled from raw input |
| `docs/backlog.md` | `document/features` | Epic → Feature → Story + AC nested |
| `docs/gap-analysis.md` | `document/analysis` | 17 standard columns (Topic / Criteria / As-Is / To-Be / Impl per role / Est per role / Decision) |
| `docs/erd.md` | `diagram/erd` | Mermaid `erDiagram` block |

Each file contains inline guidance on how to fill it. To have the agent fill one, just say `"fill docs/<file>.md from input/<source>"`.

## Common commands

```
"sync drive"           → pull latest from Google Drive into context/ (if the project has Drive sync set up)
"create story under FEAT-XX"  → agent uses the google_sheets API or edits docs/backlog.md
"gen Jira tasks"       → agent uses skill integration/jira → output/jira/*.md
"build SRS"            → agent uses skill document/srs → output/<project>-SRS.docx
"render ERD .drawio"   → agent renders Mermaid → output/<project>-ERD.drawio
```

## Need a new setup?

Project still empty? Type `/set-up` again (re-runs the bootstrap to fill what's missing).
Setting up a specific integration (e.g. Sheets API): see `MasterMind/models/model_001/integration/<skill>/SETUP.md`.

## Core Rule reminders

- ✅ `input/` = user-managed (drop files here, the agent must not write to it)
- ✅ `context/` = agent-managed (md sidecars, do not edit manually — overwritten on next sync)
- ✅ `output/` = deliverables — the agent writes, the user reads/exports
- ✅ Every binary file in `output/` must have a `.md` sidecar in `context/` (token-cheap reading)
- ❌ Do not edit `MasterMind/` — it is the skill library, synced from GitHub
