# requirements · patterns

Reusable patterns specific to requirements analysis.

| Pattern | Purpose |
|---|---|
| *(none yet)* | Add requirements-specific patterns here as they emerge |

The cross-skill pattern — the requirements table as the single source feeding the ERD and the feature list — lives with the pipeline: [`business_analysis/patterns/`](../../../business_analysis/patterns/).

## How to add a pattern

Create `<pattern-name>.md` following **Problem → Solution → Trade-offs → Worked example → When NOT to use → Cross-references**.
