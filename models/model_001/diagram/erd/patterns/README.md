# ERD · patterns

Reusable patterns specific to ERD work.

| Pattern | Purpose |
|---|---|
| *(none yet)* | Add ERD-specific patterns here as they emerge |

The cross-skill pattern that drives the ERD — the requirements table as the single source — lives with the pipeline: [`business_analysis/patterns/`](../../../business_analysis/patterns/).

## How to add a pattern

Create `<pattern-name>.md` following **Problem → Solution → Trade-offs → Worked example → When NOT to use → Cross-references**.
